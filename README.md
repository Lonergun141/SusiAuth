# 🔐 SusiAuth

> “Susi sa imong sistema.”  
> The key to your ecosystem.

SusiAuth is a reusable, production-ready authentication service built with Django and Django Ninja.  
It is designed to be plugged into multiple applications or microservices as a centralized identity provider.

---

## ✨ Features

- RS256 JWT Access Tokens
- Refresh Token Rotation
- Secure Refresh Token Storage (hashed in DB)
- Email Verification
- Forgot / Reset Password (single-use tokens)
- Change Password
- Logout (single device & all sessions)
- PostgreSQL ready
- Dockerized setup
- Versioned API (`/api/v1`)

